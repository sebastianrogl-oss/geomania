<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <uses-permission android:name="android.permission.VIBRATE"/>

    <!-- Erinnerungen (flutter_local_notifications).

         POST_NOTIFICATIONS ist ab Android 13 zwingend und wird zur Laufzeit
         erfragt (BenachrichtigungsService.systemErlaubnisAnfragen). Das Plugin
         bringt die Zeile in seinem eigenen Manifest zwar mit, sie steht hier
         trotzdem: welche Berechtigungen die App verlangt, soll aus DIESER Datei
         hervorgehen und nicht erst aus dem zusammengeführten Ergebnis.

         RECEIVE_BOOT_COMPLETED braucht der ScheduledNotificationBootReceiver
         weiter unten, um geplante Benachrichtigungen nach einem Neustart
         wiederherzustellen.

         Bewusst NICHT dabei: SCHEDULE_EXACT_ALARM / USE_EXACT_ALARM. Die
         Erinnerungen werden inexakt geplant (AndroidScheduleMode
         .inexactAllowWhileIdle) — ob sie um 19:00 oder 19:04 erscheinen, ist
         einerlei, und exakte Alarme gibt Google Play nur für Wecker und
         Kalender frei. -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS"/>
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED"/>

    <application
        android:label="geomania"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:screenOrientation="portrait"
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
        <meta-data
            android:name="com.google.android.gms.ads.APPLICATION_ID"
            android:value="ca-app-pub-4580295867570231~1622789574"/>

        <!-- Zeigt die geplante Benachrichtigung, wenn ihr Alarm ausgelöst hat.
             Ohne diesen Empfänger wird zwar geplant, aber nie etwas
             angezeigt. -->
        <receiver
            android:exported="false"
            android:name="com.dexterous.flutterlocalnotifications.ScheduledNotificationReceiver" />

        <!-- Ein Neustart löscht alle Alarme des Systems. Dieser Empfänger holt
             die geplanten Benachrichtigungen aus dem Speicher des Plugins
             zurück und meldet sie erneut an — deshalb überleben sie einen
             Neustart. MY_PACKAGE_REPLACED deckt denselben Fall nach einem
             App-Update ab. -->
        <receiver
            android:exported="false"
            android:name="com.dexterous.flutterlocalnotifications.ScheduledNotificationBootReceiver">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED"/>
                <action android:name="android.intent.action.MY_PACKAGE_REPLACED"/>
                <action android:name="android.intent.action.QUICKBOOT_POWERON" />
                <action android:name="com.htc.intent.action.QUICKBOOT_POWERON"/>
            </intent-filter>
        </receiver>
    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
    </queries>
</manifest>
